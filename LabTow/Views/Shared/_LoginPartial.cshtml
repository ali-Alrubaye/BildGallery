
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("Logout", "Authentication", new { area = "" }, FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        @*@Html.AntiForgeryToken()*@

        <ul class="nav navbar-nav navbar-right">
            <li>
                @*@Html.ActionLink("Hello " + User.Identity.GetUserName() + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })*@
                @Html.ActionLink("Hello " + @User.Identity.Name + "!", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
            </li>
            <li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
        </ul>
    }
}
else
{
    <ul class="nav navbar-nav navbar-right">
        @*<li>@Html.ActionLink("Register", "Register", "Authentication", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
            <li>@Html.ActionLink("Log in", "Login", "Authentication", routeValues: null, htmlAttributes: new {id = "loginLink"})</li>*@
        <li> @Html.ActionLink("Log In", "Login", "Authentication", new { area = "" }, null)</li>
        <li> @Html.ActionLink("Register", "Register", "Authentication", new { area = "" }, null)</li>
    </ul>
}
